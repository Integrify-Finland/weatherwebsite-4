import React from "react";

import City from "../City/City";

const Cities = (data) => {

    return (
        <div>
            <City newCity={data} />
        </div>
    )
  };
  
  export default Cities;
  